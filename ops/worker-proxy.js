const UPSTREAM="https://ard-catalog-lab.pages.dev";export default{async fetch(request){const i=new URL(request.url),u=new URL(i.pathname+i.search,UPSTREAM),h=new Headers(request.headers);h.delete("host");const r=await fetch(new Request(u,{method:request.method,headers:h,body:["GET","HEAD"].includes(request.method)?undefined:request.body,redirect:"manual"})),o=new Headers(r.headers);o.set("x-ard-catalog-origin","cloudflare-pages");return new Response(r.body,{status:r.status,statusText:r.statusText,headers:o})}};
